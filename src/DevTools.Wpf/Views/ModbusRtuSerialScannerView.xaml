<UserControl x:Class="DevTools.Wpf.Views.ModbusRtuSerialScannerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1280">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Connection" Height="76">
            <Grid Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="240"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Port" Style="{StaticResource FieldLabelStyle}"/>
                    <ComboBox x:Name="PortComboBox" Width="176" Style="{StaticResource ConnectionFieldComboStyle}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Slave From" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox x:Name="SlaveFromTextBox" Width="56" Text="1" Style="{StaticResource ConnectionFieldTextBoxStyle}"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Slave To" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox x:Name="SlaveToTextBox" Width="56" Text="32" Style="{StaticResource ConnectionFieldTextBoxStyle}"/>
                </StackPanel>

                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Address" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox x:Name="AddressTextBox" Width="64" Text="0" Style="{StaticResource ConnectionFieldTextBoxStyle}"/>
                </StackPanel>

                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                    <TextBlock Text="Timeout ms" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox x:Name="TimeoutTextBox" Width="72" Text="300" Style="{StaticResource ConnectionFieldTextBoxStyle}"/>
                </StackPanel>

                <Button x:Name="RefreshPortsButton" Grid.Column="5" Content="Refresh" Width="90" Margin="8,0,0,0" Style="{StaticResource ConnectionSecondaryButtonStyle}" Click="RefreshPortsButton_Click"/>
                <TextBlock x:Name="StatusText" Grid.Column="6" Text="Ready" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="Scan Setup" Margin="0,8,0,8" Padding="10,8,10,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="260"/>
                    <ColumnDefinition Width="400"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Read Type" Style="{StaticResource FieldLabelStyle}"/>
                    <ComboBox x:Name="ReadTypeComboBox" Width="156" Style="{StaticResource ConnectionFieldComboStyle}" SelectedIndex="2">
                        <ComboBoxItem Content="Coil (FC01)"/>
                        <ComboBoxItem Content="Discrete Input (FC02)"/>
                        <ComboBoxItem Content="Holding Register (FC03)"/>
                        <ComboBoxItem Content="Input Register (FC04)"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Baud Rates" Style="{StaticResource FieldLabelStyle}"/>
                    <WrapPanel>
                        <CheckBox x:Name="Baud9600CheckBox" Content="9600" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,8,0" IsChecked="True"/>
                        <CheckBox x:Name="Baud19200CheckBox" Content="19200" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,8,0" IsChecked="True"/>
                        <CheckBox x:Name="Baud38400CheckBox" Content="38400" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,8,0"/>
                        <CheckBox x:Name="Baud57600CheckBox" Content="57600" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,8,0"/>
                        <CheckBox x:Name="Baud115200CheckBox" Content="115200" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,8,0"/>
                    </WrapPanel>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,8,0,0">
                    <TextBlock Text="Frames" Style="{StaticResource FieldLabelStyle}"/>
                    <WrapPanel>
                        <CheckBox x:Name="Frame8N1CheckBox" Content="8N1" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,8,0" IsChecked="True"/>
                        <CheckBox x:Name="Frame8E1CheckBox" Content="8E1" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,8,0"/>
                        <CheckBox x:Name="Frame8O1CheckBox" Content="8O1" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,8,0"/>
                        <CheckBox x:Name="Frame8N2CheckBox" Content="8N2" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,8,0"/>
                    </WrapPanel>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button x:Name="StartScanButton" Content="Start Scan" Width="120" Margin="0,0,8,0" Style="{StaticResource ConnectionPrimaryButtonStyle}" Click="StartScanButton_Click"/>
                    <Button Content="Clear Results" Width="120" Style="{StaticResource ConnectionSecondaryButtonStyle}" Click="ClearResultsButton_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Scan Results" Padding="10,8,10,10">
            <DataGrid ItemsSource="{Binding Results}"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Width="130" Binding="{Binding Timestamp}"/>
                    <DataGridTextColumn Header="Slave" Width="52" Binding="{Binding SlaveId}"/>
                    <DataGridTextColumn Header="Baud" Width="74" Binding="{Binding BaudRate}"/>
                    <DataGridTextColumn Header="Frame" Width="58" Binding="{Binding Frame}"/>
                    <DataGridTextColumn Header="Type" Width="130" Binding="{Binding ReadType}"/>
                    <DataGridTextColumn Header="Address" Width="70" Binding="{Binding Address}"/>
                    <DataGridTextColumn Header="Status" Width="112" Binding="{Binding Status}"/>
                    <DataGridTextColumn Header="Value" Width="140" Binding="{Binding Value}"/>
                    <DataGridTextColumn Header="Info" Width="*" Binding="{Binding Message}"/>
                    <DataGridTextColumn Header="ms" Width="56" Binding="{Binding DurationMs}"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>
